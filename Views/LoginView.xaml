<Window x:Class="GAS.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GAS.ViewModels"
        Title="Вход" Height="260" Width="360" WindowStartupLocation="CenterScreen">
  <Window.DataContext>
    <vm:LoginViewModel/>
  </Window.DataContext>
  <Grid Margin="16">
    <StackPanel>
      <TextBlock Text="Вход" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
      <Label Content="Логин"/>
      <TextBox Text="{Binding Login}" Margin="0,0,0,8"/>
      <Label Content="Пароль"/>
      <PasswordBox x:Name="Pwd" PasswordChanged="Pwd_OnPasswordChanged" Margin="0,0,0,12"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
        <Button Content="Регистрация" Command="{Binding OpenRegisterCommand}"/>
        <Button Content="Войти" Command="{Binding LoginCommand}"/>
      </StackPanel>
    </StackPanel>
  </Grid>
</Window>

