<Window x:Class="GAS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GAS.ViewModels"
        Title="Учёт заявок" Height="700" Width="1100" WindowStartupLocation="CenterScreen">
  <Window.DataContext>
    <vm:MainViewModel/>
  </Window.DataContext>
  <DockPanel>
    <StackPanel DockPanel.Dock="Left" Width="200" Background="#f2f2f2" Padding="8">
      <TextBlock Text="Меню" FontWeight="Bold" Margin="0,0,0,8"/>
      <Button Content="Клиенты" Command="{Binding ShowClientsCommand}" Margin="0,0,0,6"/>
      <Button Content="Заявки" Command="{Binding ShowRequestsCommand}" Margin="0,0,0,6"/>
      <Button Content="Оборудование" Command="{Binding ShowEquipmentCommand}" Margin="0,0,0,6"/>
    </StackPanel>
    <Border BorderBrush="#ddd" BorderThickness="1" Margin="8">
      <ContentControl Content="{Binding CurrentView}"/>
    </Border>
  </DockPanel>
</Window>

